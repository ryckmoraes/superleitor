- name: Add Android platform (Capacitor)
        run: |
          npx cap add android || echo "already added"

      - name: Reinstall dependencies after adding android
        run: |
          rm -rf node_modules
          npm install --legacy-peer-deps

      - name: Sync Capacitor
        run: |
          npx cap sync android