
rootProject.name = 'superleitor'

include ':app'

// Capacitor plugins são resolvidos via Maven, não como projetos locais
// Removido todas as referências a projetos capacitor-* locais

def capacitorPropertiesFile = new File(settingsDir, '../capacitor.properties')
def properties = new Properties()

if (capacitorPropertiesFile.exists()) {
    capacitorPropertiesFile.withInputStream { properties.load(it) }
}

// Plugin dependencies são gerenciadas via Maven no capacitor.build.gradle
